language: rust
rust:
  - 1.28.0
  - stable
  - beta
  - nightly
script:
  - cargo build --verbose --all
  - cargo test --verbose --all
notifications:
  email:
    on_success: never
branches:
  only:
    - master